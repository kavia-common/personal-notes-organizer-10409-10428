<resources>
    <style name="Theme.NotesApp.Light" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
    </style>

    <!-- Used to set before setContentView to ensure splashless light mode -->
    <style name="Theme_NotesApp_Light_Base" parent="Theme.Material3.Light.NoActionBar"/>
</resources>
